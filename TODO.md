# Tinker TODOs

## Future Enhancements
- [ ] **Add a Favicon**: Create and add a `favicon.ico` to the `public/` directory to prevent browser 404 errors.
- [ ] **Setup Code Formatting**: Add a formatter/linter config (e.g., Biome or Prettier) to automatically format TypeScript and CSS files.
- [x] **Build Process**: Create a production build script to bundle the TypeScript into optimized JS for deployment.
- [ ] **Mobile Responsiveness**: Tweak the CSS to ensure the synth layout fits nicely on smaller screens.
- [ ] **More Presets**: Design and add more default patches to the preset dropdown.
- [ ] **MIDI Support**: Use the Web MIDI API to allow external MIDI keyboards to control the synthesizer.
